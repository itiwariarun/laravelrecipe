import React from "react";
import ReactDom from "react-dom";

function Button2(props) {
  return (
    <div className="mt-5 -mb-2">
      <button2 href={props.href} className="btn-buttony btn-button-common">
        {props.name}
      </button2>
    </div>
  );
}

export default button2;
